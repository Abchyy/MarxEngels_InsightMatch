# Corpus packages

Only schemas, rules, manifests without secrets, and synthetic fixtures belong in Git. Real PDFs, OCR output, page images and verified corpus releases must remain in configured external data storage.

The collaborator responsible for `feature/corpus-pipeline` owns the implementation under this directory and `packages/marx_engels/ingestion/`.
