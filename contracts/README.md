# Frozen contract snapshots

This directory contains generated V1 JSON Schema and OpenAPI snapshots. Source models live in `packages/marx_engels/contracts/`.

- Regenerate intentionally with `make freeze-contracts`.
- Verify without changing files with `make verify-contracts`.
- Any breaking difference requires an ADR and contract version review.
